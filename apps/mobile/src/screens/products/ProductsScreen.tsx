import { ScreenState } from '../../components/ScreenState';

export function ProductsScreen() {
  return (
    <ScreenState
      title="Ürünler"
      description="Ürün arama ve barkod lookup için ekran iskeleti hazır. Tüm API çağrıları lib/api.ts üzerinden bağlanacak."
    />
  );
}
